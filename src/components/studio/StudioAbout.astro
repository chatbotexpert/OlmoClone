---
// StudioAbout.astro
---

<section class="bg-brutal-white text-brutal-black py-32 px-8 border-b border-brutal-black/10">
  <div class="max-w-screen-xl mx-auto flex flex-col items-center text-center">
    
    <div class="mb-12 flex items-center gap-4 text-xs font-semibold tracking-widest uppercase opacity-50">
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
        <path d="M12 2.5A1.5 1.5 0 0 1 13.5 4v5.402L18.178 6.7a1.5 1.5 0 0 1 1.5 2.598L15 12l4.678 2.701a1.5 1.5 0 0 1-1.5 2.598l-4.678-2.7V20a1.5 1.5 0 0 1-3 0v-5.402L5.822 17.3a1.5 1.5 0 1 1-1.5-2.598L9 12 4.322 9.299a1.5 1.5 0 0 1 1.5-2.598l4.678 2.7V4A1.5 1.5 0 0 1 12 2.5" />
      </svg>
      <span>Our Story</span>
      <span class="px-2">—</span>
      <span>Our Story</span>
    </div>

    <h2 id="studio-about-heading" class="font-heading font-black text-3xl md:text-5xl uppercase tracking-tighter leading-none max-w-4xl mb-12 flex flex-col gap-2 md:gap-4">
      <span class="block overflow-hidden pb-2 md:pb-4"><span class="block translate-y-[120%] opacity-0">FROM VISION</span></span>
      <span class="block overflow-hidden pb-2 md:pb-4"><span class="block translate-y-[120%] opacity-0">TO IMPACT,</span></span>
      <span class="block overflow-hidden pb-2 md:pb-4"><span class="block translate-y-[120%] opacity-0">CRAFTING BRANDS</span></span>
      <span class="block overflow-hidden pb-2 md:pb-4 text-brutal-orange"><span class="block translate-y-[120%] opacity-0">THAT INSPIRE</span></span>
      <span class="block overflow-hidden pb-2 md:pb-4"><span class="block translate-y-[120%] opacity-0">AND ENDURE.</span></span>
    </h2>

    <p id="studio-about-text" class="font-body text-lg md:text-xl max-w-2xl opacity-0 translate-y-10 font-medium leading-relaxed">
      Our story is one of continuous evolution. What started as a small creative studio has blossomed into a full-service branding agency, united by a passion for design and a commitment to excellence. We believe in building digital experiences that don't just look good, but fundamentally transform how businesses connect with their audiences.
    </p>

  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  function initStudioAbout() {
    const headingLines = gsap.utils.toArray('#studio-about-heading > span > span');
    
    if (headingLines.length > 0) {
      gsap.to(headingLines, {
        scrollTrigger: {
          trigger: '#studio-about-heading',
          start: "top 80%",
          toggleActions: "play none none reverse"
        },
        y: "0%",
        opacity: 1,
        duration: 1.2,
        stagger: 0.1,
        ease: "power4.out"
      });
    }

    const text = document.getElementById('studio-about-text');
    if (text) {
      gsap.to(text, {
        scrollTrigger: {
          trigger: text,
          start: "top 85%",
          toggleActions: "play none none reverse"
        },
        y: 0,
        opacity: 0.7,
        duration: 1.2,
        ease: "power3.out",
        delay: 0.3
      });
    }
  }

  document.addEventListener('astro:page-load', initStudioAbout);
  if (document.readyState === 'complete' || document.readyState === 'interactive') {
    initStudioAbout();
  } else {
    document.addEventListener('DOMContentLoaded', initStudioAbout);
  }
</script>
